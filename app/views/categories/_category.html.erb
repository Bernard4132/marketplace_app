<p class="list-group-item">
  <i class="fa fa-angle-right" style="color: #e20d0d; font-weight: 700;" aria-hidden="true"></i>
  <%= link_to category.name, category, :style => "color: #e20d0d;" %><% if signed_in? && current_user.admin? %> <strong>|</strong> <%= link_to 'delete', category, method: :delete, data: { confirm: 'Are you sure?' } %> <% end %> 
  (<%= category.posters.count %>)
</p>